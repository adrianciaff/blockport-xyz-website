// src/components/Hero.tsx
import React from 'react';

const Hero: React.FC = () => {
  return (
    <section> {/* Minimal structure */}
      <h1>HERO COMPONENT RENDERED</h1>
    </section>
  );
};

export default Hero; // Ensure export is present